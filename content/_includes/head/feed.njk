{%- for feed in collections.__xml_feeds -%}
<link
  rel="alternate"
  type="application/{{ feed.data.feed }}"
  title="{{ feed.data.title }}: {{ site.description }}"
  href="{{ feed.url | absoluteUrl(site.url) }}" />
{%- endfor -%}
