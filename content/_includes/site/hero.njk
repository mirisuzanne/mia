{% if hero.img %}
<figure
  data-area="main">
  <div data-content="hero">
    <img
      src="{{ site.images + hero.img }}"
      alt="{{ hero.alt or '' }}"
      class="hero-image" />
    {% if hero.caption %}
    <figcaption class="hero-caption">
      {{ hero.caption | md | safe }}
    </figcaption>
    {% endif %}
  </div>
</figure>
{% endif %}
