{% import "events/macros.njk" as cal %}

{% set calendar = cal.get_calendar(
  events_group=index or renderData.calendar or calendar or series,
  collections=collections,
  page=page
) %}

{% if content %}
  {{ calendar }}
{% else %}
  {{ show.main(
    content=calendar,
    page=false
  ) }}
{% endif %}
